apiVersion: 1

providers:
- name: 'Prometheus'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  # Save the dashboard JSON file back here if you edit them.
  editable: true
  options:
    path: /etc/grafana/provisioning/dashboards
